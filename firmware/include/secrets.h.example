// Copy this file to secrets.h (git-ignored) and fill in your WiFi network.
// If secrets.h is missing, or the network cannot be joined within 10 s,
// the board opens its own access point "instrument-XXXX" / password "instrument".
#pragma once

#define WIFI_SSID     "your-ssid"
#define WIFI_PASSWORD "your-password"
