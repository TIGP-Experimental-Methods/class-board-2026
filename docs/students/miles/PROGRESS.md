# PROGRESS — Miles

- **Language:** English + Traditional Chinese (bilingual — English first, then the same content in Chinese)
- **Background:** none yet in software, electronics or CAD
- **Pace:** normal (one step at a time, with the concept behind each step explained)
- **Section:** not yet assigned (Workshop 2)

## 2026-09-20 — L1 (class)
- Toolchain check: git, gh (signed in), pio all OK; Cloudflare account connected to GitHub. ESP32 board and USB-C cable not yet in hand — needed before Project 2 Step 1, not before Project 1.

### Project 1 — Game_Breakout
- Repository: https://github.com/Miles-Lee-000/Game_Breakout (public, created and pushed by the tutor, approved by the student; renamed from `Game_breakout` to `Game_Breakout` same day — GitHub repo, local folder and README all updated)
- Vision (Step 1, written by the student): a Breakout game — paddle moves left/right only near the bottom; randomly placed bricks near the top; click/tap launches the ball in a random upward direction; walls/ceiling/bricks reflect by collision direction; paddle reflection follows a hit-position rule (center → mostly up, edges → more horizontal, avoid purely vertical/horizontal travel); bricks disappear on hit; 3 levels with different brick layouts, 3 lives; ball speeds up as bricks clear (rule to be proposed by Claude in the plan and reviewed by the student — must stay challenging late-game but controllable); "Fail" on 0 lives, "Victory" on clearing all 3 levels; HUD shows Lives / Level / Start-Pause-Restart; controls: arrow keys or A/D, mouse-follow, touch/drag; smooth motion, reliable collisions (no tunneling), paddle stays on-screen.
- Specification/Plan written; independent review (fresh Claude session in the same repo) found 14 issues (critical: degenerate-angle guard broken for exactly-vertical/horizontal hits via `Math.sign(0) === 0`; unbounded dt; ambiguous collision/speed-rescale ordering — plus 11 medium/low findings). All 14 fixed in `SPEC.md`/`PLAN.md` v2, verified by the tutor reading the files directly (not just the student's summary). Student did the hand-predictable-test exercise correctly (paddle edge-hit angle = 60°; level-1 speed cap = 450 px/s).
- Committed and pushed: `spec and plan v2: fix review findings` (verified via `git log`/`git status` — working tree clean, `main` matches `origin/main`). **Milestone: plan reviewed.**
- Code step: all 23 PLAN.md tasks completed and committed (verified via `git log`); flagged and resolved one scope-creep question along the way (student asked why a Playwright/Node dev-testing dependency appeared outside the task list — author explained it's dev-only, gitignored, needed for interactive-input testing; student chose to keep it). Pushed (25 commits; VS Code's Source Control "Sync Changes" button hit a stale-command bug, tutor ran `git push` directly as the course's terminal-git exception).
- Test/Verify: student personally playtested in browser and confirmed both hand-predicted numbers from the review stage (60° paddle-edge bounce angle; 450 px/s level-1 speed cap) against the running game. Comparison written into SPEC.md §12 "Verification", committed (`bd57d9d`) and pushed — verified by the tutor directly. **Milestone: first working version.**
- Side lesson (while Code step ran in the background): a from-zero structural walkthrough of how a web app is built — web page/browser as interpreter, HTML (structure/tags/attributes), CSS (selectors/appearance), JavaScript (event listeners, using real snippets from the student's own `main.js`), the DOM (live structure vs. static text — several follow-up questions, e.g. why not just keep HTML text in memory, well reasoned by the student), object-oriented design (DOM name), and API — all tied to real lines in the student's own repo rather than abstract examples. Paused partway (canvas lesson given but its check unanswered; "why one file / no build step," client vs. server, and what GitHub Pages hosting does are still pending) so the student could return to the actual game once Code finished — resume from there next session if the student wants to continue.
- Deployed to GitHub Pages (tutor ran `gh api ... /pages`, approved by student): app at https://miles-lee-000.github.io/Game_Breakout/app/, repository root reserved for the project page (Step 4, not yet written). **Milestone: deployed.**

## 2026-09-21 — L1 (class)
- Done: finished the first version of the Breakout game.
- Verified: checked the 60° paddle-edge bounce angle and the 450 px/s level-1 speed cap against the running game.
- Next: improve the interface — icons, background, and the game's overall visual smoothness/polish. Then, with the polished version: write the Project 1 project website (`index.html` at the `Game_Breakout` repository root) and add the class project wall entry — student explicitly chose to do both after the visual pass rather than now, so the screenshot/GIF only needs to be taken once.
- Gotchas: some bugs turned up along the way (see the Playwright question and the review's 14 findings above); VS Code's Source Control "Sync Changes" button failed silently (stale `git.sync` command) — worked around by running `git push` directly. With AI's help, problems like these get found and fixed efficiently.
